export { default } from "next-auth/middleware"

export const config = {
    matcher: [  "/pages/final/uploadImage",
                "/pages/final/result",
                "/pages/osteoarthritis",
                "/pages/scoliosis",
             ]
};